server {
    listen 3000;
    
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # Sagt Nginx: "Alles mit /api leite an den Python-Server weiter"
    location /api/ {
        proxy_pass http://api:8000/;
    }
}